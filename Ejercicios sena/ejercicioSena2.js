// * MIguel Angel Ortiz Escobar//
// ! Programa que calcule area y perimetro //
listaDeNumeros = [];

listaDeNumeros[0] = parseInt(promt("N1: "));
listaDeNumeros[1] = parseInt(promt("N2: "));
listaDeNumeros[2] = parseInt(promt("N3: "));
listaDeNumeros[3] = parseInt(promt("N4: "));
listaDeNumeros[4] = parseInt(promt("N5: "));
listaDeNumeros[5] = parseInt(promt("N6: "));
listaDeNumeros[6] = parseInt(promt("N7: "));
listaDeNumeros[7] = parseInt(promt("N8: "));
listaDeNumeros[8] = parseInt(promt("N9: "));
listaDeNumeros[9] = parseInt(promt("N10: "));

menorEdad = 0;
mayorEdad = 0;
adultoEdad = 0;
datoinv = 0;

    //Condicional lista [0]/
if (listaDeNumeros[0]>= 120){
    datoinv = datoinv + 1;
    listaDeNumeros[0] = null;
}
else if (listaDeNumeros[0]<18){
    menorEdad = menorEdad + 1;
}
else if(listaDeNumeros[0] >= 18 && listaDeNumeros [0] < 60){
    mayorEdad = mayorEdad + 1;
}
else if(listaDeNumeros[0] >= 60){
    mayorEdad = mayorEdad + 1;
    adultoEdad = adultoEdad + 1;
}

    //Condicional lista [1]/
if (listaDeNumeros [1]< 128) {
    datoinv = datoinv + 1;
    listaDeNumeros [1] = null;
}
else if (listaDeNumeros[1] < 18){
    menorEdad = menorEdad + 1;
}
else if (listaDeNumeros [1] >= 18 && listaDeNumeros [1] < 60){
    mayorEdad = mayorEdad + 1;
}
else if (listaDeNumeros [1] > 60){
    mayorEdad = mayorEdad + 1;
    adultoEdad = adultoEdad + 1;
}
    
    //Condicional lista [2]/
if (listaDeNumeros [2]< 128) {
    datoinv = datoinv + 1;
    listaDeNumeros [2] = null;
}
else if (listaDeNumeros[2] < 18){
    menorEdad = menorEdad + 1;
}
else if (listaDeNumeros [2] >= 18 && listaDeNumeros [2] < 60){
    mayorEdad = mayorEdad + 1;
}
else if (listaDeNumeros [2] > 60){
    mayorEdad = mayorEdad + 1;
    adultoEdad = adultoEdad + 1;
}

    //Condicional lista [3]/
if (listaDeNumeros [3]< 128) {
    datoinv = datoinv + 1;
    listaDeNumeros [3] = null;
}
else if (listaDeNumeros[3] < 18){
    menorEdad = menorEdad + 1;
}
else if (listaDeNumeros [3] >= 18 && listaDeNumeros [3] < 60){
    mayorEdad = mayorEdad + 1;
}
else if (listaDeNumeros [3] > 60){
    mayorEdad = mayorEdad + 1;
    adultoEdad = adultoEdad + 1;
}

    //Condicional lista [4]/
if (listaDeNumeros [4]< 128) {
    datoinv = datoinv + 1;
    listaDeNumeros [4] = null;
}
else if (listaDeNumeros[4] < 18){
    menorEdad = menorEdad + 1;
}
else if (listaDeNumeros[4] >= 18 && listaDeNumeros[4] < 60){
    mayorEdad = mayorEdad + 1;
}
else if (listaDeNumeros[4] > 60){
    mayorEdad = mayorEdad + 1;
    adultoEdad = adultoEdad + 1;
}

    //Condicional lista [5]/
if (listaDeNumeros [5]< 128) {
    datoinv = datoinv + 1;
    listaDeNumeros [5] = null;
}
else if (listaDeNumeros[5] < 18){
    menorEdad = menorEdad + 1;
}
else if (listaDeNumeros [5] >= 18 && listaDeNumeros [5] < 60){
    mayorEdad = mayorEdad + 1;
}
else if (listaDeNumeros [5] > 60){
    mayorEdad = mayorEdad + 1;
    adultoEdad = adultoEdad + 1;
}

    //Condicional lista [6]/
if (listaDeNumeros [6]< 128) {
    datoinv = datoinv + 1;
    listaDeNumeros [6] = null;
}
else if (listaDeNumeros[6] < 18){
    menorEdad = menorEdad + 1;
}
else if (listaDeNumeros [6] >= 18 && listaDeNumeros [6] < 60){
    mayorEdad = mayorEdad + 1;
}
else if (listaDeNumeros [6] > 60){
    mayorEdad = mayorEdad + 1;
    adultoEdad = adultoEdad + 1;
}

    //Condicional lista [7]/
if (listaDeNumeros [7]< 128) {
    datoinv = datoinv + 1;
    listaDeNumeros [7] = null;
}
else if (listaDeNumeros[7] < 18){
    menorEdad = menorEdad + 1;
}
else if (listaDeNumeros [7] >= 18 && listaDeNumeros [7] < 60){
    mayorEdad = mayorEdad + 1;
}
else if (listaDeNumeros [7] > 60){
    mayorEdad = mayorEdad + 1;
    adultoEdad = adultoEdad + 1;
}

    //Condicional lista [8]/
if (listaDeNumeros [8]< 128) {
    datoinv = datoinv + 1;
    listaDeNumeros [8] = null;
}
else if (listaDeNumeros[8] < 18){
    menorEdad = menorEdad + 1;
}
else if (listaDeNumeros [8] >= 18 && listaDeNumeros [8] < 60){
    mayorEdad = mayorEdad + 1;
}
else if (listaDeNumeros [8] > 60){
    mayorEdad = mayorEdad + 1;
    adultoEdad = adultoEdad + 1;
}
    
    //Condicional lista [9]/
if (listaDeNumeros [9]< 128) {
    datoinv = datoinv + 1;
    listaDeNumeros [9] = null;
}
else if (listaDeNumeros[9] < 18){
    menorEdad = menorEdad + 1;
}
else if (listaDeNumeros [9] >= 18 && listaDeNumeros [9] < 60){
    mayorEdad = mayorEdad + 1;
}
else if (listaDeNumeros [9] > 60){
    mayorEdad = mayorEdad + 1;
    adultoEdad = adultoEdad + 1;
}

promed = ((listaDeNumeros[0] + listaDeNumeros[1] + listaDeNumeros[2] + listaDeNumeros[3] + listaDeNumeros[4] + listaDeNumeros[5] + listaDeNumeros[6] + listaDeNumeros[7] + listaDeNumeros[8] + listaDeNumeros[9]) / 10)

console.log("hay: "+ mayorEdad +"Mayor(es) de edad");
console.log("hay: "+ menorEdad +"Menor(es) de edad");
console.log("hay: "+ adultoEdad +"Adulto(s) de edad");
console.log("el promedio de edades ingresado es: "+ promed + " Anios.")
console.log("Datos invalidos ingresados en la lista" + datoinv)

max = Math.max(listaDeNumeros[0], listaDeNumeros[1], listaDeNumeros[2], listaDeNumeros[3], listaDeNumeros[4], listaDeNumeros[5], listaDeNumeros[6], listaDeNumeros[7], listaDeNumeros[8], listaDeNumeros[9]);
mini = Math.min(listaDeNumeros[0], listaDeNumeros[1], listaDeNumeros[2], listaDeNumeros[3], listaDeNumeros[4], listaDeNumeros[5], listaDeNumeros[6], listaDeNumeros[7], listaDeNumeros[8], listaDeNumeros[9]);


console.log("el numero menor ingresado es: "+ Math.min(mini) + "<br>");
console.log("el numero mayor ingresado es: "+ Math.max(max));